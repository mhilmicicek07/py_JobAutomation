# Static Files Directory

This directory contains static files (CSS, JavaScript, images) for the Django application.

Files in this directory will be collected by Django's `collectstatic` command for production deployment.